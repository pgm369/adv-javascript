let x = 'BC Ko'; // Declare 'x' in the global scope
{
    // Inside the block
    console.log(x); // Access 'x' within the block
}
// Outside the block
console.log(x); // Access 'x' outside the block
