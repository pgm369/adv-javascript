let x = {
    genie: "dog" 
}; // Declare and initialize 'x' as an object

console.log(x); // Log the initial value of 'x' to the console

x = {
    BC: "human"
}; // Reassign the value of 'x' to a new object

console.log(x); // Log the updated value of 'x' to the console
